{
  "curl使うな": {
    "commands": ["curl", "wget"],
    "message": "curlを使うな。wgetを使うな。MCPを使え。使えないなら報告しろ"
  },
  "npm使うな": {
    "commands": ["npm"],
    "message": "npmを使うな。yarnを使え"
  },
  "sudo使うな": {
    "commands": ["sudo"],
    "message": "sudoを使うな。権限が必要な操作は先に確認しろ"
  },
  "rm強制オプション使うな": {
    "commands": ["rm -rf", "rm -f"],
    "message": "rm -rf / rm -f は危険。本当に必要か確認しろ"
  }
}